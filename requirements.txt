# Dependencies for src/, scripts/, and notebooks/ (dataset preparation,
# training, evaluation, report generation). For the web app's runtime
# dependencies only, see backend/requirements.txt.

tensorflow-cpu>=2.16    # swap for `tensorflow` if you have a GPU set up
pandas>=2.0
numpy>=1.26
scikit-learn>=1.3
imbalanced-learn>=0.12
pillow>=10.0
matplotlib>=3.8
tabulate>=0.9

# testing
pytest>=8.0
